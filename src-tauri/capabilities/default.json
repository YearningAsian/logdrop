{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permission set for the main window: core defaults plus the file-open dialog. No filesystem plugin, no shell, no network.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
